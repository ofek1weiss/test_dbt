version: 2
models:
  - name: all_dates
    columns:
      - name: date_day
        tests:
          - dbt_utils.equal_rowcount:
              compare_model: ref('other_table_name')
              config: {}
              meta: {}
