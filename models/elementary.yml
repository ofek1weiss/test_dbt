version: 2
models:
  - name: all_dates
    columns:
      - name: date_day
        tests:
          - dbt_utils.not_empty_string:
              trim_whitespace: true # Optional
              config:
                tags:
                  - blabla
              meta:
                owner:
                  - ofek@elementary-data.com
